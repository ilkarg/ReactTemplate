import { ButtonModel } from "../../Models";
import "./styles/Button.sass";

export const Button = (props: ButtonModel) => {
    return (
        <>
            <button>test</button>
        </>
    );
}
